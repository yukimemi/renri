# renri is a publishable APM package — `apm install yukimemi/renri`
# resolves to a tag matching this `version`. Bump it together with
# Cargo.toml / Cargo.lock when releasing a new tag.
name: renri
version: 0.1.13
description: 'Skill describing how to use the renri CLI — a unified manager for git

  worktrees and jujutsu workspaces, configured via teravars-powered TOML.

  Install with `apm install yukimemi/renri` to make this skill available

  to Copilot, Claude Code, Cursor, OpenCode, Codex, and Gemini at once.

  '
dependencies:
  apm: []
